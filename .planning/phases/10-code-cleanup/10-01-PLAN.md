---
phase: 10-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/auth/sync-user.ts
  - app/api/usage/route.ts
  - app/api/db-health/route.ts
autonomous: true

must_haves:
  truths:
    - "getCurrentUser() function no longer exists in codebase"
    - "/api/usage POST endpoint has JSDoc explaining internal/debug purpose"
    - "/api/db-health endpoint has JSDoc explaining ops-only purpose"
  artifacts:
    - path: "lib/auth/sync-user.ts"
      provides: "syncUser function (getCurrentUser removed)"
      contains: "export async function syncUser"
    - path: "app/api/usage/route.ts"
      provides: "Usage API with documented POST endpoint"
      contains: "@internal Debug endpoint"
    - path: "app/api/db-health/route.ts"
      provides: "Health check API with documentation"
      contains: "@internal Ops-only"
  key_links: []
---

<objective>
Remove orphaned code and document internal endpoints identified in v2.0 milestone audit.

Purpose: Clean up tech debt to maintain code quality and prevent confusion about unused exports.
Output: Cleaner auth module without dead code, documented internal endpoints.
</objective>

<execution_context>
@C:\Users\milos\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\milos\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v2.0-MILESTONE-AUDIT.md

Source files:
@lib/auth/sync-user.ts
@app/api/usage/route.ts
@app/api/db-health/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove getCurrentUser function</name>
  <files>lib/auth/sync-user.ts</files>
  <action>
    Remove the `getCurrentUser()` function (lines 67-98) from lib/auth/sync-user.ts.

    This function is exported but never imported anywhere in the codebase.
    The `syncUser()` function handles all user lookup/creation needs.

    Delete the entire function including its JSDoc comment:
    ```
    /**
     * Gets the current user from our database (without creating).
     * Returns null if not authenticated or not in database.
     */
    export async function getCurrentUser(): Promise<SyncedUser | null> {
      // ... entire function body
    }
    ```

    Keep the file ending clean (no trailing blank lines beyond one).
  </action>
  <verify>
    1. Run: grep -r "getCurrentUser" lib/ app/ --include="*.ts" --include="*.tsx"
       Should return NO results (function removed and was never imported)
    2. Run: npx tsc --noEmit
       Should compile without errors
  </verify>
  <done>getCurrentUser function no longer exists in sync-user.ts, TypeScript compiles successfully</done>
</task>

<task type="auto">
  <name>Task 2: Document POST /api/usage as internal endpoint</name>
  <files>app/api/usage/route.ts</files>
  <action>
    Update the JSDoc comment for the POST handler to indicate it's an internal/debug endpoint.

    Current JSDoc (line 65-73):
    ```
    /**
     * POST /api/usage - Increment usage counter
     *
     * Returns:
     * - 200: { allowed: true, used, limit, resetAt } - Increment successful
     * - 429: { allowed: false, used, limit, resetAt } - Limit reached
     * - 401: { error: 'Unauthorized' } - Not authenticated
     * - 404: { error: 'User not found' } - User not in database
     * - 500: { error: 'Internal server error' } - Unexpected error
     */
    ```

    Update to:
    ```
    /**
     * POST /api/usage - Increment usage counter
     *
     * @internal Debug endpoint - not called by frontend.
     * Usage increment is handled directly in /api/generate to ensure atomicity.
     * This endpoint exists for manual testing and debugging purposes.
     *
     * Returns:
     * - 200: { allowed: true, used, limit, resetAt } - Increment successful
     * - 429: { allowed: false, used, limit, resetAt } - Limit reached
     * - 401: { error: 'Unauthorized' } - Not authenticated
     * - 404: { error: 'User not found' } - User not in database
     * - 500: { error: 'Internal server error' } - Unexpected error
     */
    ```
  </action>
  <verify>
    1. Read app/api/usage/route.ts and confirm @internal tag is present in POST JSDoc
    2. Run: npx tsc --noEmit
       Should compile without errors
  </verify>
  <done>POST /api/usage has @internal JSDoc tag explaining it's a debug endpoint</done>
</task>

<task type="auto">
  <name>Task 3: Document /api/db-health as ops endpoint</name>
  <files>app/api/db-health/route.ts</files>
  <action>
    Add JSDoc comment to the GET handler explaining this is an ops-only health check.

    Current code has no JSDoc. Add before the GET function:
    ```
    /**
     * GET /api/db-health - Database health check
     *
     * @internal Ops-only endpoint for infrastructure monitoring.
     * Used to verify database connectivity and schema access.
     * Not intended for frontend consumption.
     *
     * Returns:
     * - 200: { status: 'healthy', database: 'connected', ... } - DB accessible
     * - 503: { status: 'unhealthy', error: string } - DB connection failed
     */
    ```
  </action>
  <verify>
    1. Read app/api/db-health/route.ts and confirm @internal tag is present
    2. Run: npx tsc --noEmit
       Should compile without errors
  </verify>
  <done>/api/db-health has @internal JSDoc tag explaining it's an ops-only endpoint</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Orphaned code removed:**
   ```bash
   grep -r "getCurrentUser" . --include="*.ts" --include="*.tsx" | grep -v node_modules | grep -v ".planning"
   ```
   Should return empty (no occurrences in source code)

2. **TypeScript compiles:**
   ```bash
   npx tsc --noEmit
   ```
   Should exit with code 0

3. **Internal endpoints documented:**
   - app/api/usage/route.ts POST handler has @internal JSDoc
   - app/api/db-health/route.ts GET handler has @internal JSDoc
</verification>

<success_criteria>
1. getCurrentUser() function removed from lib/auth/sync-user.ts
2. POST /api/usage documented with @internal tag indicating debug purpose
3. GET /api/db-health documented with @internal tag indicating ops purpose
4. TypeScript compilation succeeds
5. No orphaned exports remain in auth module
</success_criteria>

<output>
After completion, create `.planning/phases/10-code-cleanup/10-01-SUMMARY.md`
</output>
