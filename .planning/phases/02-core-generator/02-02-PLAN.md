---
phase: 02-core-generator
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/PostConfiguration.tsx
  - app/layout.tsx
autonomous: true

must_haves:
  truths:
    - "User can select tone from 4 options (Profesionalus/Draugiškas/Motyvuojantis/Humoristinis)"
    - "User can select emoji usage from 3 options (Taip/Ne/Minimaliai)"
    - "User can select post length from 3 options (Trumpas/Vidutinis/Ilgas)"
    - "Toast notifications appear when actions succeed or fail"
  artifacts:
    - path: "app/components/PostConfiguration.tsx"
      provides: "Form controls for tone, emoji, and length settings"
      exports: ["PostConfiguration", "PostConfigurationValues"]
    - path: "app/layout.tsx"
      provides: "Toaster provider for toast notifications"
      contains: "<Toaster"
  key_links:
    - from: "app/layout.tsx"
      to: "react-hot-toast"
      via: "Toaster component import"
      pattern: "import.*Toaster.*from.*react-hot-toast"
---

<objective>
Create post configuration component with tone/emoji/length settings and set up toast notifications.

Purpose: Enable users to customize post generation with tone (professional/friendly/motivational/humorous), emoji usage (yes/no/minimal), and length (short/medium/long). Toast notifications provide feedback on user actions.

Output: Reusable PostConfiguration component and Toaster setup in layout for app-wide toast support.
</objective>

<execution_context>
@C:\Users\milos\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\milos\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-generator/02-RESEARCH.md
@app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install react-hot-toast and configure Toaster in layout</name>
  <files>
    package.json
    app/layout.tsx
  </files>
  <action>
    1. Install react-hot-toast:
       ```bash
       npm install react-hot-toast
       ```

    2. Update `app/layout.tsx` to include Toaster provider:
       - Import Toaster from react-hot-toast
       - Add Toaster component inside body, positioned at bottom-center
       - Toaster should be after {children} to ensure proper stacking

    Example layout update:
    ```tsx
    import { Toaster } from 'react-hot-toast';

    export default function RootLayout({ children }: { children: React.ReactNode }) {
      return (
        <html lang="lt">
          <body className={inter.className}>
            {children}
            <Toaster
              position="bottom-center"
              toastOptions={{
                duration: 2000,
                style: {
                  background: '#333',
                  color: '#fff',
                },
              }}
            />
          </body>
        </html>
      );
    }
    ```

    Note: Keep existing layout structure (html lang, body className, etc.)
  </action>
  <verify>
    - `npm run lint` passes
    - `npm run build` passes
    - Toaster import and component present in app/layout.tsx
  </verify>
  <done>
    - react-hot-toast in package.json dependencies
    - Toaster component in layout with bottom-center position
    - Toast duration set to 2000ms (2 seconds per context requirement)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create PostConfiguration component</name>
  <files>
    app/components/PostConfiguration.tsx
  </files>
  <action>
    Create `app/components/PostConfiguration.tsx`:
    - 'use client' directive at top
    - Export interface PostConfigurationValues with tone, emoji, length fields
    - Props: values (PostConfigurationValues), onChange callback
    - Three select/radio groups:

    1. Tone selection:
       - Options: 'professional' | 'friendly' | 'motivational' | 'humorous'
       - Lithuanian labels: Profesionalus, Draugiškas, Motyvuojantis, Humoristinis
       - Use radio buttons or styled button group for better mobile UX

    2. Emoji usage:
       - Options: 'yes' | 'no' | 'minimal'
       - Lithuanian labels: Taip, Ne, Minimaliai

    3. Length selection:
       - Options: 'short' | 'medium' | 'long'
       - Lithuanian labels: Trumpas, Vidutinis, Ilgas

    Implementation approach:
    - Use button groups instead of native selects for better touch targets
    - Each option button: flex, px-4 py-2, rounded, border
    - Selected state: bg-blue-600 text-white, unselected: bg-white text-gray-700
    - Group options horizontally on mobile (flex-wrap if needed)
    - Include Lithuanian labels above each group

    Example structure:
    ```tsx
    'use client';

    export interface PostConfigurationValues {
      tone: 'professional' | 'friendly' | 'motivational' | 'humorous';
      emoji: 'yes' | 'no' | 'minimal';
      length: 'short' | 'medium' | 'long';
    }

    interface Props {
      values: PostConfigurationValues;
      onChange: (values: PostConfigurationValues) => void;
    }

    export function PostConfiguration({ values, onChange }: Props) {
      const handleChange = (key: keyof PostConfigurationValues, value: string) => {
        onChange({ ...values, [key]: value });
      };

      return (
        <div className="space-y-4">
          {/* Tone */}
          <div>
            <label className="block text-sm font-medium mb-2">Tonas</label>
            <div className="flex flex-wrap gap-2">
              {/* Button for each tone option */}
            </div>
          </div>
          {/* Similar for emoji and length */}
        </div>
      );
    }
    ```

    Button option helper (DRY):
    ```tsx
    function OptionButton({ selected, onClick, children }) {
      return (
        <button
          type="button"
          onClick={onClick}
          className={`px-4 py-2 rounded border text-sm font-medium transition-colors ${
            selected
              ? 'bg-blue-600 text-white border-blue-600'
              : 'bg-white text-gray-700 border-gray-300 hover:border-gray-400'
          }`}
        >
          {children}
        </button>
      );
    }
    ```
  </action>
  <verify>
    - `npm run lint` passes
    - `npm run build` passes
    - File exists at app/components/PostConfiguration.tsx
    - PostConfigurationValues type exported
    - PostConfiguration component exported
  </verify>
  <done>
    - PostConfiguration component renders three option groups
    - Tone has 4 options with Lithuanian labels
    - Emoji has 3 options with Lithuanian labels
    - Length has 3 options with Lithuanian labels
    - onChange callback fires when user selects any option
    - Selected state visually distinct (blue background)
    - Touch-friendly button targets (px-4 py-2 minimum)
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run `npm run build` - should pass without errors
2. Run `npm run lint` - should pass without errors
3. Verify file structure:
   - app/layout.tsx contains Toaster component
   - app/components/PostConfiguration.tsx exists
4. Check exports:
   - PostConfiguration component
   - PostConfigurationValues interface
</verification>

<success_criteria>
- react-hot-toast in package.json dependencies
- Toaster in layout.tsx with bottom-center position, 2000ms duration
- PostConfiguration component accepts values and onChange props
- Three settings groups (tone, emoji, length) with Lithuanian labels
- Button-style options with clear selected state
- Components compile without TypeScript errors
- Components are 'use client' and ready for integration
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-generator/02-02-SUMMARY.md`
</output>
