---
phase: 06-post-history
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/supabase/storage.ts
  - lib/posts/queries.ts
autonomous: true

must_haves:
  truths:
    - "Supabase Storage bucket 'post-images' exists and accepts uploads"
    - "DALL-E images can be downloaded and re-uploaded to permanent storage"
    - "Posts can be queried with cursor-based pagination"
    - "Posts can be filtered by favorites and searched by text"
  artifacts:
    - path: "lib/supabase/storage.ts"
      provides: "Supabase Storage upload helper for DALL-E images"
      exports: ["uploadDalleImage"]
    - path: "lib/posts/queries.ts"
      provides: "Drizzle query helpers for posts CRUD"
      exports: ["getUserPosts", "getPostById", "createPost", "toggleFavorite", "softDeletePost"]
  key_links:
    - from: "lib/supabase/storage.ts"
      to: "Supabase Storage API"
      via: "supabase.storage.from('post-images').upload()"
      pattern: "storage\\.from\\('post-images'\\)"
    - from: "lib/posts/queries.ts"
      to: "app/db/schema.ts"
      via: "drizzle query on posts table"
      pattern: "from\\(posts\\)"
---

<objective>
Create foundational storage and query helpers for post history feature.

Purpose: Establish the data layer (storage + queries) that all subsequent plans depend on. Critical: DALL-E URLs expire in 60 minutes, so we must immediately download and upload to permanent Supabase Storage.

Output:
- `lib/supabase/storage.ts` - Upload DALL-E images to Supabase Storage
- `lib/posts/queries.ts` - Drizzle query helpers for posts CRUD with cursor pagination
</objective>

<execution_context>
@C:\Users\milos\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\milos\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-post-history/06-RESEARCH.md

# Existing code
@app/db/schema.ts
@lib/supabase/server.ts
@lib/supabase/client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Supabase Storage helper for DALL-E images</name>
  <files>lib/supabase/storage.ts</files>
  <action>
Create `lib/supabase/storage.ts` with `uploadDalleImage` function:

1. Import createClient from '@supabase/supabase-js' (use service role key for server-side uploads)
2. Create function `uploadDalleImage(dalleUrl: string, userId: string, fileId: string): Promise<string>`
3. Implementation:
   - Fetch image from DALL-E URL using `fetch(dalleUrl)`
   - Convert response to blob with `response.blob()`
   - Generate filename: `${userId}/${fileId}-${Date.now()}.png`
   - Upload to 'post-images' bucket with content type 'image/png'
   - Get public URL using `getPublicUrl()`
   - Return the permanent public URL

4. Use environment variables:
   - `NEXT_PUBLIC_SUPABASE_URL` (existing)
   - `SUPABASE_SERVICE_ROLE_KEY` (need to add to .env.local)

5. Export the function for use in API routes.

IMPORTANT: The bucket 'post-images' must be created in Supabase Dashboard. Add a note about this in the summary.

Error handling:
- Throw descriptive errors if fetch fails or upload fails
- Log errors to console for debugging
  </action>
  <verify>
TypeScript compiles without errors:
```bash
npx tsc --noEmit lib/supabase/storage.ts
```
  </verify>
  <done>
- `lib/supabase/storage.ts` exists with `uploadDalleImage` export
- Function accepts dalleUrl, userId, fileId and returns Promise<string>
- Uses Supabase Storage API correctly
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Drizzle query helpers for posts</name>
  <files>lib/posts/queries.ts</files>
  <action>
Create `lib/posts/queries.ts` with query helpers for posts CRUD:

1. Import from drizzle-orm: `eq`, `isNull`, `desc`, `lt`, `and`, `ilike`
2. Import db from `@/app/db` and posts/users from `@/app/db/schema`
3. Define `PAGE_SIZE = 20` constant

4. Create `getUserPosts` function:
   ```typescript
   export async function getUserPosts(
     userId: number,
     cursor?: number,
     search?: string,
     favoritesOnly?: boolean
   ): Promise<{ posts: Post[]; nextCursor: number | null }>
   ```
   - Build conditions array: userId match, deletedAt is null
   - Add cursor condition if provided (id < cursor)
   - Add favorites filter if favoritesOnly is true
   - Add ILIKE search if search provided
   - Order by id DESC (newest first)
   - Limit to PAGE_SIZE + 1 (to detect if more exist)
   - Return posts and nextCursor (last post id if more exist, null otherwise)

5. Create `getPostById` function:
   ```typescript
   export async function getPostById(postId: number, userId: number): Promise<Post | null>
   ```
   - Query by id AND userId (security: user can only see their own posts)
   - Filter deletedAt is null
   - Return single post or null

6. Create `createPost` function:
   ```typescript
   export async function createPost(
     userId: number,
     text: string,
     imageUrl: string | null,
     config: GenerationConfig
   ): Promise<{ id: number }>
   ```
   - Insert into posts table
   - Return the new post id using `.returning({ id: posts.id })`

7. Create `toggleFavorite` function:
   ```typescript
   export async function toggleFavorite(postId: number, userId: number): Promise<boolean>
   ```
   - First get current isFavorite value
   - Update to opposite value (0 -> 1, 1 -> 0)
   - Return new favorite status

8. Create `softDeletePost` function:
   ```typescript
   export async function softDeletePost(postId: number, userId: number): Promise<void>
   ```
   - Update deletedAt to current timestamp
   - Only for posts matching userId (security)

Export all functions. Add TypeScript types for Post (or use inferred type from schema).
  </action>
  <verify>
TypeScript compiles without errors:
```bash
npx tsc --noEmit lib/posts/queries.ts
```
  </verify>
  <done>
- `lib/posts/queries.ts` exists with all 5 functions exported
- `getUserPosts` implements cursor-based pagination with PAGE_SIZE + 1 trick
- `getUserPosts` supports search (ILIKE) and favorites filter
- All functions include `isNull(deletedAt)` filter
- All functions check userId for authorization
  </done>
</task>

</tasks>

<verification>
1. Both files compile without TypeScript errors
2. Storage helper uses Supabase Storage API correctly
3. Query helpers use Drizzle ORM patterns from existing codebase
4. All queries filter by `isNull(deletedAt)` (soft delete pattern)
5. Cursor pagination uses `lt(posts.id, cursor)` pattern
</verification>

<success_criteria>
- `lib/supabase/storage.ts` exports `uploadDalleImage` function
- `lib/posts/queries.ts` exports 5 query helper functions
- TypeScript compiles both files without errors
- Patterns match existing codebase conventions
</success_criteria>

<output>
After completion, create `.planning/phases/06-post-history/06-01-SUMMARY.md`

Include in summary:
- User setup required: Create 'post-images' bucket in Supabase Dashboard
- User setup required: Add SUPABASE_SERVICE_ROLE_KEY to .env.local
</output>
