---
phase: 05-authentication
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/sign-in/[[...sign-in]]/page.tsx
  - app/sign-up/[[...sign-up]]/page.tsx
autonomous: true

must_haves:
  truths:
    - "Sign-in page displays Clerk SignIn component"
    - "Sign-up page displays Clerk SignUp component"
    - "Both pages use catch-all routes for Clerk's multi-step flows"
    - "Google OAuth button visible on both pages"
    - "Email/password form visible on both pages"
    - "Magic link option available on sign-in"
  artifacts:
    - path: "app/sign-in/[[...sign-in]]/page.tsx"
      provides: "Sign-in page with Clerk component"
      contains: "SignIn"
    - path: "app/sign-up/[[...sign-up]]/page.tsx"
      provides: "Sign-up page with Clerk component"
      contains: "SignUp"
  key_links:
    - from: "app/sign-in/[[...sign-in]]/page.tsx"
      to: "Clerk UI"
      via: "@clerk/nextjs SignIn component"
      pattern: "SignIn"
    - from: "app/sign-up/[[...sign-up]]/page.tsx"
      to: "Clerk UI"
      via: "@clerk/nextjs SignUp component"
      pattern: "SignUp"
---

<objective>
Create sign-in and sign-up pages using Clerk's pre-built components with catch-all routes.

Purpose: Provide authentication UI for Google OAuth, email/password, and magic link login. Clerk components handle all the complex auth flows (OAuth redirects, password validation, email verification, magic link delivery) automatically.

Output: Sign-in page at /sign-in with Google OAuth + email/password + magic link options. Sign-up page at /sign-up with Google OAuth + email/password options.
</objective>

<execution_context>
@C:\Users\milos\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\milos\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-authentication/05-RESEARCH.md
@.planning/phases/05-authentication/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sign-in page with Clerk SignIn component</name>
  <files>app/sign-in/[[...sign-in]]/page.tsx</files>
  <action>
Create the sign-in page using Clerk's SignIn component:

```typescript
// app/sign-in/[[...sign-in]]/page.tsx
import { SignIn } from '@clerk/nextjs'

export default function SignInPage() {
  return (
    <div className="flex min-h-screen items-center justify-center bg-gray-50">
      <SignIn
        appearance={{
          elements: {
            // Match existing app styling
            formButtonPrimary:
              'bg-blue-600 hover:bg-blue-700 text-sm normal-case',
            card: 'shadow-lg rounded-lg',
            headerTitle: 'text-2xl font-bold text-gray-900',
            headerSubtitle: 'text-gray-600',
            socialButtonsBlockButton:
              'border border-gray-300 hover:bg-gray-50 text-gray-700',
            socialButtonsBlockButtonText: 'font-medium',
            formFieldLabel: 'text-sm font-medium text-gray-700',
            formFieldInput:
              'rounded-md border-gray-300 focus:border-blue-500 focus:ring-blue-500',
            footerActionLink: 'text-blue-600 hover:text-blue-700',
            dividerLine: 'bg-gray-200',
            dividerText: 'text-gray-500 text-sm',
          },
        }}
      />
    </div>
  )
}
```

NOTES:
- Catch-all route [[...sign-in]] is REQUIRED for Clerk's multi-step flows (password reset, 2FA, etc.)
- Clerk automatically shows Google OAuth + email/password + magic link based on Dashboard settings
- The appearance prop uses Tailwind classes to match the app's styling
- SignIn handles all auth methods configured in Clerk Dashboard automatically
  </action>
  <verify>
Check file exists and compiles:
```bash
npx tsc --noEmit app/sign-in/[[...sign-in]]/page.tsx 2>&1 || echo "Check TS errors"
```
Check SignIn component is used:
```bash
grep -c "SignIn" "app/sign-in/[[...sign-in]]/page.tsx"
```
  </verify>
  <done>Sign-in page created at app/sign-in/[[...sign-in]]/page.tsx with Clerk SignIn component and Tailwind styling</done>
</task>

<task type="auto">
  <name>Task 2: Create sign-up page with Clerk SignUp component</name>
  <files>app/sign-up/[[...sign-up]]/page.tsx</files>
  <action>
Create the sign-up page using Clerk's SignUp component:

```typescript
// app/sign-up/[[...sign-up]]/page.tsx
import { SignUp } from '@clerk/nextjs'

export default function SignUpPage() {
  return (
    <div className="flex min-h-screen items-center justify-center bg-gray-50">
      <SignUp
        appearance={{
          elements: {
            // Match existing app styling (same as sign-in for consistency)
            formButtonPrimary:
              'bg-blue-600 hover:bg-blue-700 text-sm normal-case',
            card: 'shadow-lg rounded-lg',
            headerTitle: 'text-2xl font-bold text-gray-900',
            headerSubtitle: 'text-gray-600',
            socialButtonsBlockButton:
              'border border-gray-300 hover:bg-gray-50 text-gray-700',
            socialButtonsBlockButtonText: 'font-medium',
            formFieldLabel: 'text-sm font-medium text-gray-700',
            formFieldInput:
              'rounded-md border-gray-300 focus:border-blue-500 focus:ring-blue-500',
            footerActionLink: 'text-blue-600 hover:text-blue-700',
            dividerLine: 'bg-gray-200',
            dividerText: 'text-gray-500 text-sm',
          },
        }}
      />
    </div>
  )
}
```

NOTES:
- Same styling as sign-in page for visual consistency
- Clerk automatically shows Google OAuth + email/password based on Dashboard settings
- Email verification is optional (configured in Clerk Dashboard per CONTEXT.md decision)
- SignUp handles account creation, OAuth linking, and email verification flows
  </action>
  <verify>
Check file exists and compiles:
```bash
npx tsc --noEmit app/sign-up/[[...sign-up]]/page.tsx 2>&1 || echo "Check TS errors"
```
Check SignUp component is used:
```bash
grep -c "SignUp" "app/sign-up/[[...sign-up]]/page.tsx"
```
  </verify>
  <done>Sign-up page created at app/sign-up/[[...sign-up]]/page.tsx with Clerk SignUp component and Tailwind styling</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Both sign-in and sign-up pages exist with catch-all routes
2. TypeScript compiles without errors
3. Both pages use Clerk components with consistent styling
4. Structure follows Next.js 15 App Router conventions

Manual verification (after Plan 01 Clerk config is complete):
1. Visit /sign-in - Clerk sign-in form appears
2. Visit /sign-up - Clerk sign-up form appears
3. Google OAuth button visible on both pages
4. Email/password fields visible on both pages
</verification>

<success_criteria>
- app/sign-in/[[...sign-in]]/page.tsx exists with SignIn component
- app/sign-up/[[...sign-up]]/page.tsx exists with SignUp component
- Both use catch-all routes for Clerk's multi-step flows
- Styling matches app's visual language (blue primary buttons, gray accents)
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-authentication/05-02-SUMMARY.md`
</output>
