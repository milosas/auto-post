---
phase: 09-dashboard-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/dashboard/queries.ts
  - app/components/StatCard.tsx
  - app/dashboard/layout.tsx
  - app/dashboard/page.tsx
autonomous: true

must_haves:
  truths:
    - "User sees dashboard after login with clear navigation"
    - "Dashboard shows generations today count"
    - "Dashboard shows total posts saved count"
    - "Dashboard shows favorites count"
    - "Dashboard shows subscription or credit status clearly"
    - "Dashboard provides quick access to generate new post"
  artifacts:
    - path: "lib/dashboard/queries.ts"
      provides: "Parallel stats fetching with soft-delete filtering"
      exports: ["getDashboardStats"]
    - path: "app/components/StatCard.tsx"
      provides: "Reusable stat card component"
      exports: ["StatCard"]
    - path: "app/dashboard/layout.tsx"
      provides: "Dashboard layout with navigation"
      min_lines: 30
    - path: "app/dashboard/page.tsx"
      provides: "Dashboard page with stats grid"
      min_lines: 50
  key_links:
    - from: "app/dashboard/page.tsx"
      to: "lib/dashboard/queries.ts"
      via: "getDashboardStats function call"
      pattern: "getDashboardStats"
    - from: "app/dashboard/page.tsx"
      to: "app/components/StatCard.tsx"
      via: "StatCard component import"
      pattern: "StatCard"
    - from: "app/dashboard/layout.tsx"
      to: "app/components/AuthHeader.tsx"
      via: "AuthHeader component import"
      pattern: "AuthHeader"
---

<objective>
Create a user dashboard showing usage stats (generations today, total posts saved, favorites) and subscription/credit status with navigation between dashboard, generator, and history.

Purpose: Users need a centralized view of their account status and quick access to main features after login.
Output: Working dashboard at /dashboard with stats cards, subscription status, and navigation.
</objective>

<execution_context>
@C:\Users\milos\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\milos\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-dashboard-polish/09-CONTEXT.md
@.planning/phases/09-dashboard-polish/09-RESEARCH.md

# Existing patterns to follow
@app/db/schema.ts
@app/db/index.ts
@lib/posts/queries.ts
@app/api/usage/route.ts
@app/api/subscription/status/route.ts
@app/components/AuthHeader.tsx
@lib/stripe/subscriptions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboard stats query helper</name>
  <files>lib/dashboard/queries.ts</files>
  <action>
Create lib/dashboard/queries.ts with getDashboardStats function:

1. Import db from @/app/db, schema tables (posts, usageLimits, subscriptions), and drizzle operators (eq, count, and, isNull)

2. Define DashboardStats type:
   - generationsToday: number
   - totalPosts: number
   - favorites: number
   - subscription: { status: string, credits: number, plan?: string } | null

3. Implement getDashboardStats(userId: number, timezone: string):
   - Use Promise.all for parallel queries (per RESEARCH.md pitfall #4):
     a. Total posts: count from posts WHERE userId = ? AND deletedAt IS NULL
     b. Favorites: count from posts WHERE userId = ? AND isFavorite = 1 AND deletedAt IS NULL
     c. Usage: select from usageLimits WHERE userId = ? LIMIT 1
     d. Subscription: select from subscriptions WHERE userId = ? LIMIT 1
   - Return DashboardStats object with computed values
   - Handle null cases (0 for counts, null for subscription)

Important: Always filter deletedAt IS NULL per RESEARCH.md pitfall #6 (deleted records).
  </action>
  <verify>
TypeScript compiles without errors:
```bash
npx tsc --noEmit lib/dashboard/queries.ts
```
  </verify>
  <done>
getDashboardStats function exists, exports DashboardStats type, uses parallel queries with soft-delete filtering.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create StatCard component and dashboard layout</name>
  <files>app/components/StatCard.tsx, app/dashboard/layout.tsx</files>
  <action>
**StatCard Component (app/components/StatCard.tsx):**
Create a client component ('use client' not needed - pure presentational):

```typescript
interface StatCardProps {
  title: string;
  value: number | string;
  subtitle?: string;
  icon?: React.ReactNode;
}
```

Styling (per RESEARCH.md pattern):
- Container: bg-white rounded-lg shadow-sm border border-gray-200 p-6 hover:shadow-md transition-shadow
- Title: text-sm font-medium text-gray-600
- Value: text-3xl font-bold text-gray-900 mt-3
- Subtitle (optional): text-sm text-gray-500 mt-2

**Dashboard Layout (app/dashboard/layout.tsx):**
Create layout with navigation:

1. Import Link from 'next/link', AuthHeader from @/app/components/AuthHeader

2. Structure:
   - min-h-screen bg-gray-50
   - Navigation bar (bg-white border-b border-gray-200):
     - Container mx-auto px-4
     - Flex items-center justify-between h-16
     - Left side: Nav links (Dashboard, Generate Post, History, Pricing)
     - Right side: AuthHeader component
   - Main content area: container mx-auto px-4 py-8 with {children}

3. Nav links styling:
   - Dashboard: text-gray-900 font-semibold (active state)
   - Others: text-gray-600 hover:text-gray-900
   - Use Link component with href="/dashboard", "/", "/history", "/pricing"

4. Lithuanian labels:
   - "Valdymo skydas" (Dashboard)
   - "Kurti irasa" (Generate Post)
   - "Istorija" (History)
   - "Kainos" (Pricing)
  </action>
  <verify>
Visit http://localhost:3000/dashboard - should show navigation bar with layout structure (even if page.tsx doesn't exist yet, layout should render).
  </verify>
  <done>
StatCard component exists with proper props and styling. Dashboard layout shows navigation with 4 links and AuthHeader.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create dashboard page with stats grid</name>
  <files>app/dashboard/page.tsx</files>
  <action>
Create app/dashboard/page.tsx as a Server Component (no 'use client'):

1. Imports:
   - createClient from @/lib/supabase/server
   - redirect from next/navigation
   - db from @/app/db
   - users from @/app/db/schema
   - eq from drizzle-orm
   - getDashboardStats from @/lib/dashboard/queries
   - StatCard from @/app/components/StatCard
   - Link from next/link

2. Authentication check:
   - Create Supabase client, get user
   - If not authenticated, redirect('/sign-in')
   - Get internal user ID from users table (auth ID -> internal ID pattern)

3. Fetch stats:
   - Get timezone from headers or default to 'Europe/Vilnius' (Lithuanian users)
   - Call getDashboardStats(userId, timezone)

4. Page structure:
   - Header section:
     - h1: "Valdymo skydas" (text-2xl font-bold text-gray-900)
     - Generate button: Link to "/" with gradient styling (bg-gradient-to-r from-blue-600 to-purple-600 text-white px-6 py-3 rounded-lg hover:from-blue-700 hover:to-purple-700)
     - Text: "Kurti nauja irasa" with icon (+ symbol or arrow)

   - Stats grid:
     - Responsive grid: grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mt-8
     - 4 StatCard components:
       a. "Sugeneruota siandien" - stats.generationsToday
       b. "Issaugoti irasai" - stats.totalPosts
       c. "Megstami" - stats.favorites
       d. Subscription card (special handling below)

   - Subscription card logic:
     - If stats.subscription?.status === 'active': show "Pro" with green badge
     - If stats.subscription?.credits > 0: show credits count
     - Else: show "Nemokamas" (Free) with "Atnaujinti" link to /pricing

5. Lithuanian text (use proper characters with diacritics where appropriate):
   - "Valdymo skydas" - Dashboard
   - "Kurti nauja irasa" - Generate new post
   - "Sugeneruota siandien" - Generated today
   - "Issaugoti irasai" - Saved posts
   - "Megstami" - Favorites
   - "Prenumerata" - Subscription
   - "Kreditai" - Credits
   - "Nemokamas" - Free
   - "Atnaujinti" - Upgrade
  </action>
  <verify>
1. Run dev server: npm run dev
2. Login at http://localhost:3000/sign-in
3. Navigate to http://localhost:3000/dashboard
4. Verify:
   - 4 stat cards displayed in responsive grid
   - Navigation links work (Dashboard, Generate, History, Pricing)
   - Generate button links to home page
   - Stats show correct values (may be 0 for new users)
  </verify>
  <done>
Dashboard page shows 4 stat cards (generations today, total posts, favorites, subscription status), has navigation to generator/history/pricing, and requires authentication.
  </done>
</task>

</tasks>

<verification>
1. Authentication: Unauthenticated users visiting /dashboard are redirected to /sign-in
2. Stats display: All 4 stats render correctly (generations today, total posts, favorites, subscription)
3. Navigation: Links to /, /history, /pricing work correctly
4. Responsive: Grid collapses from 4 cols to 2 to 1 on smaller screens
5. Generate CTA: "Kurti nauja irasa" button links to home page generator
</verification>

<success_criteria>
- [ ] lib/dashboard/queries.ts exists with getDashboardStats function
- [ ] app/components/StatCard.tsx exists with proper props interface
- [ ] app/dashboard/layout.tsx exists with navigation and AuthHeader
- [ ] app/dashboard/page.tsx exists as Server Component with auth check
- [ ] Dashboard shows 4 stat cards with correct data
- [ ] Navigation between dashboard, generator, history, pricing works
- [ ] Responsive grid layout (4 -> 2 -> 1 columns)
- [ ] All text in Lithuanian
</success_criteria>

<output>
After completion, create `.planning/phases/09-dashboard-polish/09-01-SUMMARY.md`
</output>
