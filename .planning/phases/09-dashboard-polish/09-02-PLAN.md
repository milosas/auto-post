---
phase: 09-dashboard-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/IndustryAutocomplete.tsx
autonomous: true

must_haves:
  truths:
    - "User can click on selected industry value to re-open dropdown"
    - "Dropdown shows full industry list when clicked (not filtered)"
    - "User can change selection without clearing first"
    - "Keyboard navigation still works (Tab, Escape, Arrow keys)"
  artifacts:
    - path: "app/components/IndustryAutocomplete.tsx"
      provides: "Industry autocomplete with click-to-reopen behavior"
      contains: "handleInputClick"
  key_links:
    - from: "app/components/IndustryAutocomplete.tsx"
      to: "input onClick"
      via: "handleInputClick function"
      pattern: "onClick.*handleInputClick"
---

<objective>
Fix industry selector UX so clicking on a selected industry value re-opens the dropdown with the full list, allowing users to change their selection without clearing first.

Purpose: Users currently cannot change their industry selection by clicking on it - they must clear the field first. This is unintuitive combobox behavior.
Output: Modified IndustryAutocomplete.tsx with click-to-reopen functionality.
</objective>

<execution_context>
@C:\Users\milos\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\milos\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-dashboard-polish/09-CONTEXT.md
@.planning/phases/09-dashboard-polish/09-RESEARCH.md

# Component to modify
@app/components/IndustryAutocomplete.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add click-to-reopen handler to IndustryAutocomplete</name>
  <files>app/components/IndustryAutocomplete.tsx</files>
  <action>
Modify app/components/IndustryAutocomplete.tsx to add click-to-reopen behavior:

1. Add new handleInputClick function (per RESEARCH.md pattern):
```typescript
const handleInputClick = () => {
  // When user clicks input with existing value, re-open dropdown with full list
  if (value && !isOpen) {
    setIsOpen(true);
    setShowAll(true);
  }
};
```

2. Add onClick handler to the input element:
```typescript
<input
  ref={inputRef}
  type="text"
  value={value}
  onChange={(e) => {
    setShowAll(false);
    onChange(e.target.value);
  }}
  onClick={handleInputClick}  // NEW: Re-open on click when populated
  onFocus={handleFocus}
  onBlur={handleBlur}
  onKeyDown={handleKeyDown}
  placeholder="Pasirinkite savo industrija..."
  className="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent outline-none"
/>
```

3. Verify existing handlers remain intact:
   - handleFocus: Sets showAll=true, isOpen=true, highlightedIndex=0
   - handleBlur: Closes dropdown after 200ms delay
   - handleKeyDown: Arrow navigation, Enter selection, Escape close

Important per RESEARCH.md pitfall #5 (keyboard accessibility):
- Do NOT prevent default browser behavior in onClick
- Ensure onBlur still fires after clicking (the 200ms timeout handles this)
- Test that Tab, Escape, Arrow keys still work after adding onClick
  </action>
  <verify>
1. Run dev server: npm run dev
2. Go to http://localhost:3000
3. Select an industry from dropdown (e.g., "Grožio specialistai")
4. Click on the input field with the selected value
5. Verify:
   - Dropdown re-opens showing ALL industries
   - Can select a different industry
   - Can still type to filter
   - Escape key closes dropdown
   - Tab key moves to next field
   - Arrow keys navigate list
  </verify>
  <done>
IndustryAutocomplete has onClick handler that re-opens dropdown with full list when clicking on populated input. Keyboard navigation still works.
  </done>
</task>

</tasks>

<verification>
1. Click behavior: Clicking on populated input opens dropdown with full list
2. Selection change: Can select different industry without clearing first
3. Keyboard nav: Tab, Escape, Arrow keys all work correctly
4. Blur behavior: Clicking outside still closes dropdown
5. No regressions: Original focus/blur/typing behavior unchanged
</verification>

<success_criteria>
- [ ] handleInputClick function added to component
- [ ] onClick={handleInputClick} added to input element
- [ ] Clicking selected value re-opens dropdown
- [ ] Dropdown shows full list (showAll=true) when clicked
- [ ] Keyboard navigation still works (Tab, Escape, Arrows, Enter)
- [ ] No regressions in existing autocomplete behavior
</success_criteria>

<output>
After completion, create `.planning/phases/09-dashboard-polish/09-02-SUMMARY.md`
</output>
